import { bootstrapApplication } from '@angular/platform-browser';

import { InterviewSchedulerComponent } from './app/interview-scheduler.component';

bootstrapApplication(InterviewSchedulerComponent);